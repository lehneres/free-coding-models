---
title: Web Dashboard
description: A real-time browser control center for the same catalog — full parity with the TUI, with the catalog dashboard on port 3333 or the full dashboard + Smart Router on port 19280.
section: Surfaces (core)
---

The Web Dashboard is a real-time, browser-based control center for the same catalog — full parity with the TUI for everything that's safe to port. It's the best surface for browsing at a glance, sharing filtered views, or running FCM headless in Docker.

## Start it

```bash
# Catalog-only dashboard on http://localhost:3333 (override with FCM_WEB_PORT)
free-coding-models web

# Full dashboard + Smart Model Router on http://localhost:19280
free-coding-models --daemon
```

| Mode | Port | What you get |
|------|------|--------------|
| `web` (or `--web` / `--gui`) | `3333` (`FCM_WEB_PORT`) | The realtime catalog dashboard only — browse, filter, benchmark. |
| `--daemon` | `19280` (`FCM_PORT`) | Dashboard **plus** the Smart Model Router API (`/v1/...`) on the same port. |
| `--daemon-bg` | `19280` | Same as `--daemon`, but detached so it keeps running after the TUI closes. |

> 💡 Open the dashboard with `open http://localhost:3333` (or `19280`), or drive it headless with `chrome-devtools`.

---

## What's in the dashboard

The model table uses **100% of the viewport width** under a sticky header + filter bar, and every TUI capability ships behind a button or chip.

| Area | Highlights |
|---|---|
| **Header** | Logo + version · nav (Dashboard, Settings, Analytics, Recommend, Router) · kebab menu (Help, Changelog, Install Endpoints, Installed Models) · endpoint target picker · `⌘K` palette · AI Latency toggle · theme · export |
| **Model table** | 17 resizable columns (widths saved in localStorage), ⭐ star + 🔌 install per row, medal borders for top-3, dark-red rows for tool-incompatible models, click an AI Latency cell to run a per-row benchmark, sticky header |
| **Filter bar** | Tier / Status / Verdict / Health chip rows · Visibility (Normal / Configured only / Usable only) · Provider select · text filter with `X` clear · Reset · ping mode (Speed / Normal / Slow / Forced) · "next ping in Xs" countdown |
| **Detail panel** | Slide-in on row click · install-endpoint + per-row benchmark · favorite toggle + reorder · latency trend chart · all stats |
| **Command palette** | `⌘K` / `Ctrl+P` — the only global shortcut. Fuzzy search across views, theme, ping mode, reset, export, **and the full TUI command registry** |
| **Smart Recommend** | The 3-question wizard → Top 3 shared-score recommendations with Pin + install actions |
| **Router Dashboard** | Daemon start/stop, model health table with circuit-breaker badges, request log, probe-mode selector, "Probe all" benchmark, and a **Test Router** mini-playground |
| **Token Usage** | (inside Analytics) today + all-time summary, 7-day bar chart, top models & providers breakdown |
| **Settings parity** | Theme (auto/dark/light), favorites pinned mode, Startup AI Speed Scan, shell-env export, per-provider **Test key** button — all persisted to the same `~/.free-coding-models.json` the TUI uses |

> The Router Dashboard is only available in `--daemon` mode (port 19280), since the catalog-only `web` mode does not run the routing engine.

---

## Keyboard & deep-linking

**Keyboard:** `Esc` closes any modal · `Cmd+K` toggles the palette. Everything else is mouse-first.

{/* AGENT TODO: delete the <Callout> below once `website/public/videos/web-url-deep-linking.mp4` is committed. */}
<Video name="web-url-deep-linking" caption="URL deep-linking — filter the dashboard, share the URL, open it anywhere." />

<Callout type="warning" title="🎬 Recording script — delete this Callout when the video file is added">

**Setup** — Web Dashboard open in a browser (fullscreen, ~1200px wide). A few models visible.

**Shot list (~10s, no audio)**
1. **0:00–0:03** — Click the **tier** filter (e.g. **S+**) and a sort header (e.g. **Verdict**). The table updates, and the URL in the address bar updates.
2. **0:03–0:05** — Copy the URL from the address bar (⌘L, ⌘C).
3. **0:05–0:07** — Open a new tab, paste the URL (⌘V, Enter).
4. **0:07–0:10** — The dashboard loads with the **same filters applied**. Hold 2–3s.

**Export** — MP4 H.264, 720p (or 1280px-wide), 30fps, no audio, 10–15s, < 2 MB.

**When committed at `website/public/videos/web-url-deep-linking.mp4`, DELETE this `<Callout>`**.
</Callout>

**URL deep-linking:** query params hydrate the dashboard on load **and** every filter/sort/view change is reflected back into the URL (debounced). CLI flags become shareable links:

```
http://localhost:3333/?tier=S+&sort=verdict&origin=groq&toolMode=goose&q=…
```

Favorites are shared with the TUI through the same `~/.free-coding-models.json` config file — star a model in one surface and it's pinned in the other.

---

## Go further

- **Run it in Docker** without installing Node.js → [Docker Server](/docs/integrations/docker-web)
- **The routing engine behind the Router Dashboard** → [Smart Model Router](/docs/core/router-daemon)
- **REST API endpoints** (`/v1/...`, `/api/...`) → [REST API Specification](/docs/reference/rest-api)
